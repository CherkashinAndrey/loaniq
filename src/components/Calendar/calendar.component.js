import React from 'react';

import classes from './calendar.scss';

const Calendar = ( props ) => {
    return (
        <div className={classes.pageUpdates}>
            Calendar
        </div>
    );
};

export default Calendar;